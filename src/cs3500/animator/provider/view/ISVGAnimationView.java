package cs3500.animator.provider.view;

/**
 * This is where we would put in all the methods that would be required
 * for each of various SVG animation views for the future. For example, if we
 * wanted to create an interactive SVG we would make that class implement
 * this and then add the interactive methods for all the svg animation implementations to have.
 */
public interface ISVGAnimationView extends IAnimationView {

}
